<li>
  <%= gravatar_for user, size: Settings.gravatar.user.size %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %> |
    <%= link_to t("users.index.delete"), user,
      method: :delete, data: {confirm: t("users.index.confirm_delete")} %>
  <% end %>
</li>
